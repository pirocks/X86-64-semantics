// Autogenerated using stratification.
requires "x86-configuration.k"
requires "x86-flag-checks.k"
module CVTSS2SI-R64-XMM
  imports X86-CONFIGURATION
  imports X86-FLAG-CHECKS
  rule <k>
    execinstr (cvtss2si R1:Xmm, R2:R64,  .Operands) => .
  ...</k>
    <regstate>
RSMap:Map => updateMap(RSMap,
convToRegKeys(R2) |-> cvt_single_to_int64(extractMInt( getParentValue(R1, RSMap), 224, 256))
)

    </regstate>
    
endmodule

module CVTSS2SI-R64-XMM-SEMANTICS
  imports CVTSS2SI-R64-XMM
endmodule
